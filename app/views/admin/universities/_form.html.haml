= simple_form_for([:admin, @university]) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :mascot
    = f.input :location
    = f.input :image

  .form-actions
    = f.button :submit
