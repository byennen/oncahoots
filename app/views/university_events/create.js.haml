- if @event.errors.blank?
  window.location.reload();
- else
  $("#event-popup").html("#{escape_javascript(render partial: 'form', locals: {event: @event})}");
  $("#add-event").click();
  $('#event-popup .datepicker').datepicker({ dateFormat: "mm/dd/yyyy" });