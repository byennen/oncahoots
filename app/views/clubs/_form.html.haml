#create_club.modal.fade{"aria-hidden" => "true", "aria-labelledby" => "myModalLabel", :role => "dialog", :tabindex => "-1"}
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", :type => "button"}
        %h4#myModalLabel.modal-title Create Club
      / modal-header
      .modal-body
        - club.errors.full_messages.each do |msg|
          .red= msg
          %br
        = form_for [@university, club], html: {class: "forms"}, remote: true do |f|
          .half
            .col-md-12
              %label.col-md-3 Club Name
              .col-md-8= f.text_field :name
            .clearfix
            .col-md-12
              %label.col-md-3 Private
              .col-md-8= f.check_box :private
            .col-md-12
              %label.col-md-3 Category
              .col-md-6= f.select :category, options_for_select(Club::CATEGORIES), class: "gentle"
              .clearfix
            .col-md-12
              %label.col-md-3 Image
              .col-md-8= f.file_field :image
          / half
          .clearfix
          %label.ne Description
          = f.text_area :description, class: "texta"
          %button.btng{:name => "btn", type: "submit"} Create Club
        .clearfix
:javascript
  $(document).ready(function(){
    $('select#club_category').gentleSelect();
  });