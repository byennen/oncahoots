$("##{@resource.class.name.downcase}#{@resource.id}-comments span#cm-list").append("#{escape_javascript(render partial: "comment", locals: {comment: @comment})}");
$("##{@resource.class.name.downcase}#{@resource.id}-comments #comment_comment").val("");