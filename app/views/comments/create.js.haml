$("##{@resource.class.name.downcase}#{@resource.id}-comments .cm-list").append("#{escape_javascript(render partial: "comment", locals: {comment: @comment})}");
$("textarea").val("");
$('input[type=submit]').removeAttr('disabled');